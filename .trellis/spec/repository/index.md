# 仓库工作流规范

本目录记录适用于整个仓库的 Git 分支与同步约定。

## 规范索引

| 文档 | 用途 |
| --- | --- |
| [本地补丁分支与 Rebase](./git-rebase-workflow.md) | 保持官方基线干净，并将本地补丁重放到官方最新提交之上 |

## 开发前检查

- 确认当前分支是 `local-patches`，不要直接在 `main` 上开发本地补丁。
- 确认工作区干净；执行 rebase 前必须提交或暂存当前改动。
- 更新官方代码时先执行 `git fetch origin`，不要用未经确认的远程分支作为基线。

